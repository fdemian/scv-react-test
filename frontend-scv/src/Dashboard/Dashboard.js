import React from 'react';
import PropTypes from 'prop-types';

const Dashboard = ({}) => {
  return (
    <div></div>
  );
}

/*
Dashboard.propTypes = {
  : PropTypes.
};
*/

export default Dashboard;
